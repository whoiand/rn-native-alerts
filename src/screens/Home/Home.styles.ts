import {StyleSheet} from 'react-native';

const homeStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  button: {
    width: '80%',
    padding: 20,
    borderWidth: 1,
    borderColor: '#000000',
    borderRadius: 10,
    alignItems: 'center',
  },
});

export {homeStyles};
